# 라이크웨이 자료실 (DocBox) — 제안서 카톡 전달 앱

## 한 줄 정의
제안서(PDF/PPT)를 한곳에 모아두고, 필요할 때 검색해서 **버튼 한 번으로 카톡에 보내는** 개인용 웹앱.
폰에서 주로 쓴다. 화면은 3개뿐: **문서함 / 업로드 / 공유**.

## 기술 스택 (변경 금지)
- Next.js (App Router, TypeScript) + Tailwind
- Supabase (DB + Storage)
- Vercel 배포
- PWA (홈 화면에 추가해서 앱처럼 사용)

## 카톡으로 보내는 방식 (핵심)
1. **폰 (기본)**: 공유 버튼 → 브라우저 공유시트(navigator.share)에 **파일 자체**를 실어 보냄 → 카톡 선택 → 끝. 카카오 API 필요 없음.
2. **PC 또는 공유시트 미지원**: 공유 버튼 → 공유 링크(`/s/[id]`)를 클립보드에 복사 + "카카오톡으로 보내기" 버튼(Kakao JS SDK, 링크 카드 전송). 받는 사람은 카톡 안에서 바로 PDF를 열어볼 수 있음.
- 두 방식 모두 항상 노출하지 말고, **기기를 감지해서 되는 방식 하나만** 크게 보여줄 것.

## 데이터
Supabase 테이블 `documents`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid | 기본키, 공유 링크에 그대로 사용 |
| title | text | 문서 제목 (업로드 시 파일명에서 자동 채움, 수정 가능) |
| file_path | text | Storage 경로 |
| file_type | text | pdf / pptx / docx 등 |
| file_size | int | 바이트 |
| tags | text[] | 예: ["그라인드","백화점","2026"] |
| memo | text | 짧은 메모 — 목록에서 수수료율 옆에 회색 알약으로 보인다. 10자까지 (검색 대상) |
| is_favorite | bool | 즐겨찾기 |
| sent_count | int | 보낸 횟수 |
| last_sent_at | timestamptz | 마지막 보낸 시각 |
| created_at | timestamptz | |
| fee_rate | numeric(5,2) | 수수료율(%). 비어 있으면 표시 안 함 (`supabase/fee-rate.sql`) |

추가 컬럼 `folder_id` (uuid, `folders.id` 참조, null 허용 = 분류 안 함). 폴더를 지워도 문서는 남고 `분류 안 함`으로 빠진다.

Supabase 테이블 `folders`
| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid | 기본키 |
| name | text | 폴더 이름 (같은 부모 안에서만 중복 불가) |
| parent_id | uuid | 상위 폴더. 비어 있으면 맨 위 (지우면 하위도 cascade 삭제) |
| sort_order | int | 표시 순서 |
| created_at | timestamptz | |

처음 만들어 두는 폴더: 제품소개서 / 체크리스트(벤더용) / 체크리스트(셀러용)

Storage 버킷 `docs` — 파일명은 `uuid.확장자`로 저장 (추측 불가능한 이름). 공개 읽기 허용.

## 로그인
- 팀 기능 없음. 환경변수 `APP_PASSWORD` 하나로 잠금 (쿠키 세션). Supabase Auth 쓰지 말 것.
- `/s/[id]` 공유 페이지는 로그인 없이 누구나 열 수 있음.

## 화면
### 1. 자료실 `/`  (첫 화면) — 화면 제목은 "라이크웨이 자료실", 경로 표시에서는 "자료실"
- 상단 검색창 하나 (제목 + 태그 + 메모 + **폴더 이름** 동시 검색, 타이핑 즉시 필터)
- **초성 검색**: `ㅅㄷㅅ` → 소다산. 띄어 쓴 낱말은 모두 들어 있어야 한다 (`lib/format.ts` `matchesQuery`)
- **검색은 폴더를 무시하고 항상 전체를 뒤진다** (10초 목표를 위해)
- 첫 화면 머리말: 골드색 영어 제목 `LIKEWAY DOCBOX` 만 (한글 '자료실' 제목은 사용자가 빼 달라고 했다) + 요약(문서 N개 · 폴더 N개).
  검색창에는 돋보기 아이콘.
- 첫 화면은 **폴더 타일 3열**(아이콘 + 이름) + 그 아래 **'★ 즐겨찾기'**
  (사용자가 ★ 한 문서만, 최대 8개, 각 줄에 카톡 버튼·폴더 경로·보낸 횟수).
  **자주 보낸 문서·최근 문서를 자동으로 띄우지 말 것** (사용자가 숨겨 달라고 했다).
  즐겨찾기가 없으면 아무것도 보여주지 않는다 (안내 문구도 사용자가 불필요하다고 지웠다).
  즐겨찾기 문서는 미리 받아 둔다(`prefetchForShare` — 절약 모드·느린 망이면 안 받고,
  와이파이면 30MB 이하 4개, 그 외엔 5MB 이하 2개)
- 폴더는 폰 화면 안에 스크롤 없이 한 번에 다 보여야 한다
- 폴더 목록은 어디서나 가나다순
- 폴더를 누르면 그 폴더 문서만 (`/?f=<폴더id>`). 뒤로가기로 폴더 목록 복귀
- 폴더는 **3단계까지** (1차/2차/3차). 문서 하나는 폴더 하나에만 속함
- 폴더 칸 끝의 [+ 새 폴더] 로 만든다. 폴더 안에서 누르면 그 폴더의 하위폴더가 된다
  (3단계에서는 버튼이 안 보임)
- **폴더 타일·폴더 줄에 문서 개수를 표시하지 않는다** (사용자가 빼 달라고 했다). 개수 계산은 폴더 삭제 확인창에서만 쓴다
- 2단계 이상에서는 제목 아래 경로 표시 (문서함 › 제품소개서), 각각 눌러 이동
- 뒤로가기는 한 단계 위 폴더로
- 폴더를 지우면 하위폴더도 같이 지워지지만 **문서와 파일은 절대 안 지워지고** '분류 안 함' 으로 감
- 폴더 이름은 같은 위치 안에서만 겹치지 않으면 된다
- 폴더 목록의 [편집]에서 폴더 추가·이름변경·삭제
- **폴더 안에서는 그 폴더 내용만 보여준다.** 다른 폴더(형제 폴더)를 같이 띄우지 말 것
- 하위폴더가 없으면 폴더 칸 자체를 안 보여준다. [+ 새 폴더]는 목록 아래 작은 글자 버튼
- 폴더 안에서는 태그 칩들로 한 번 더 좁히기 (누르면 필터)
- 목록 정렬 기본값은 **가나다순** (즐겨찾기 → 숫자 → 한글 → 영문).
  사용자가 파일명 앞에 번호를 붙여 쓰므로 숫자는 숫자로 비교한다 (1, 2, 3 … 10, 11 — `localeCompare(…, { numeric: true })`)
- 헤더의 [가나다순/최신순] 버튼으로 전환. 즐겨찾기는 두 방식 모두 맨 위 고정.
  고른 방식은 브라우저에 기억됨
- 각 카드: **파일 종류 아이콘**(문서 모양 + 색 띠: PDF 빨강 / PPT 주황 / WORD 파랑 / EXCEL 초록 / 한글 청록),
  제목, 태그, 날짜 · 파일 크기, **카카오 노란색 카톡 버튼**
  (폰은 44px 동그란 아이콘만 — 크면 제목 자리를 뺏는다. PC 는 글자 포함 작은 알약 모양.
  PC 목록에는 크기 열 + 날짜 열)
  (#FEE500 바탕 + 말풍선 아이콘. 누르면 폰은 공유시트, PC 는 링크 복사)
- 공유 링크로 받는 파일 이름은 uuid 가 아니라 `제목.확장자` (Storage `?download=` 사용)
- **영상**(mp4/mov/m4v/webm)은 문서와 똑같이 다룬다: 보라색 ▶ 아이콘, 누르면 새 탭에서 바로 재생,
  공유 페이지 `/s/[id]` 에서는 `<video controls>` 로 그 자리에서 재생 + [다운로드].
  카톡 보내기도 문서와 같다(공유시트에 영상 파일을 그대로 싣는다)
- 문서 제목을 누르면 **중간 페이지 없이 파일이 바로** 새 탭에서 열린다 (`viewUrl`). PDF·영상 은 파일 주소 그대로,
  **엑셀·워드·PPT·한글은 제목이 붙은 다운로드 주소**로 열어 기기가 직접 연다
  (Office 온라인 뷰어는 10초 넘게 '로드 중'이라 안 열리는 것처럼 보였다 — 다시 쓰지 말 것).
  `/s/[id]` 는 받는 사람용 페이지로만 쓴다
- **수수료율 + 메모 알약**: `15%`(골드) 옆에 `부가세 별도`(회색)가 작은 알약으로 붙는다 (값이 있을 때만).
  제목과 같은 줄에 `flex-wrap` 으로 흘린다 — **이름이 짧으면 이름 옆에, 길면 알약만 아랫줄로** 내려간다.
  폰·PC 같은 방식이다. 알약을 `shrink-0` 으로 한 줄에 고정하면 긴 파일명이 잘리므로(실제로 잘렸다) 그렇게 하지 말 것.
  메모는 `MAX_NOTE_LEN`(10자)까지만 받고, 그래도 넘치면 `truncate` 로 … 처리한다.
  넣는 곳은 업로드 화면의 파일 줄마다 있는 `수수료 %` + `메모` 칸, 그리고 ⋯ → 수정 창.
  **메모는 파일마다 다르므로 업로드 화면에서도 줄마다 따로 받는다** (전체 공통 메모칸은 없앴다).
  체크리스트 브랜드 폴더의 문서에 쓰는 값이지만, **폴더 이름으로 가르지 않고** 값이 있으면 어디서나 보여 준다.
  수수료율은 `0~100` 밖이거나 비면 없는 값으로 본다 (`parseFeeRate`). 표시는 `formatFeeRate` (12.50 → `12.5%`)
- 긴 **영문 파일명**도 두 줄로 접히게 `[overflow-wrap:anywhere]` 를 제목에 함께 둔다
- 보내기에 성공하면 '✓ 보냈습니다' 토스트
- 카드 점 세개 메뉴: 제목·수수료율·태그·메모 수정, 폴더 이동, 즐겨찾기, 삭제.
  **저장이 실패하면 이유를 창 안에 띄운다** — 창 뒤 본문에만 띄우면 가려서 안 보인다
- **삭제 = 휴지통으로 이동** (`documents.deleted_at`, 파일은 그대로). 첫 화면 아래 '휴지통 N'(`/?f=trash`)에서
  복원 / 완전 삭제 / 비우기. 30일 지나면 접속 시 자동으로 파일까지 정리(`purgeExpiredTrash`).
  휴지통에 있는 문서는 공유 링크(`/s/[id]`)로도 열리지 않는다.
  파일을 실제로 지우는 코드는 반드시 `deleted_at is not null` 조건을 함께 건다.
- 우하단 플로팅 버튼: 업로드(폰). **홈(첫 화면)에는 올리기 버튼을 두지 않는다** — 떠 있는 +, 툴바 [업로드],
  가운데 올리기 영역 모두 폴더 안에서만 보인다 (문서는 폴더에 들어가서 올린다). **좌하단 플로팅 홈 버튼**: 첫 화면으로 (첫 화면에서는 숨김, PC 에서도 표시)
- '나가기'(로그아웃) 버튼은 두지 않는다

### 2. 업로드 `/upload`
- 파일 선택 (여러 개 동시 가능, pdf/ppt/pptx/doc/docx/xlsx/hwp + **짧은 영상** mp4/mov/m4v/webm)
- **파일 하나에 50MB까지.** Supabase 저장소가 정확히 거기까지만 받는다(51MB는 거부된다).
  올리기 전에 브라우저에서 걸러서 "50MB보다 커서 올릴 수 없습니다"라고 알려 준다 (`MAX_FILE_BYTES`).
  긴 영상을 넣으려면 Supabase 를 유료 플랜으로 올려야 한다 (무료: 파일 50MB·전체 1GB)
- 폴더 선택은 **드롭다운 하나**(하위폴더는 들여쓰기). 폴더가 늘어나도 화면이 안 번진다
- 제목 자동 채움 (파일마다), **수수료율·메모도 파일마다** 따로. 태그는 올리는 파일 전체에 똑같이 들어간다
- 파일은 서버를 거치지 않고 브라우저에서 Supabase Storage 로 직접 올린다
  (서버는 1회용 서명 URL 만 발급. Vercel 요청 용량 4.5MB 제한 회피)
- 저장 → 문서함으로 돌아감

### 3. 공유 페이지 `/s/[id]`  (받는 사람이 보는 화면)
- 제목, 파일 크기
- PDF: 브라우저 내장 뷰어로 바로 표시
- PPT/기타: 큰 [다운로드] 버튼 (+ 가능하면 Office 온라인 뷰어 링크)
- 로그인 없음, 군더더기 없음

## 공유 버튼 동작 (정확히 이 순서)
1. `navigator.canShare({ files })` 지원 → 파일을 fetch해서 `navigator.share({ files, title })`
2. 미지원 → 공유 링크 클립보드 복사 + 토스트 "링크 복사됨" + [카카오톡으로 보내기] 버튼 노출 (`Kakao.Share.sendDefault`, feed 타입, 버튼 링크 = `/s/[id]`)
   - 폰은 "누른 직후"에만 공유창을 허용한다. 큰 파일은 받는 동안 그 시간이 지나므로
     받은 파일을 메모리에 기억해 두고(`lib/share.ts`), 막히면 버튼을 [보내기]로 바꿔
     한 번 더 누르면 즉시 공유창이 뜨게 한다.
3. 어느 쪽이든 성공 시 `sent_count +1`, `last_sent_at` 갱신
4. **여러 개 한 번에 보내기**: 폴더 안 [선택] 버튼 → 카드 체크 → 하단 [한 번에 보내기]
   → `navigator.share({ files: [...] })` 로 파일 여러 개를 한 번에 전송.
   용량 초과로 거절되면 링크 여러 개를 줄바꿈으로 복사

## 환경변수 (.env.local)
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
APP_PASSWORD=
NEXT_PUBLIC_KAKAO_JS_KEY=      # 2번 방식용, 없으면 카카오 버튼 숨김
NEXT_PUBLIC_APP_URL=           # 배포 주소, 공유 링크 만들 때 사용
```

## 마일스톤 (순서대로, 하나 끝날 때마다 멈추고 확인받기)
- [x] **M1 뼈대**: 프로젝트 생성, Supabase 연결, 테이블·버킷 SQL 작성(`supabase/schema.sql`), 비밀번호 잠금 페이지. 로컬 실행 확인.
- [x] **M2 업로드+문서함**: 업로드 화면, 문서함 목록, 검색·태그 필터, 수정·삭제. 실제 PDF 3개 넣어보고 확인.
- [x] **M3 공유**: 공유 페이지 `/s/[id]`, 공유 버튼(폰 공유시트 → 카톡 전송 실제 테스트), 링크복사 + 카카오 버튼.
- [x] **M4 마무리**: PWA 설정(아이콘, manifest), Vercel 배포, 폰 홈화면에 추가해서 끝까지 한 번 사용.

## 디자인 (라이크웨이 톤 — office.likeway.co.kr 과 같은 값)
- 색: 크림 바탕 `#faf6ee`(bg-cream) · 종이 `#fffdf9`(bg-paper, 카드·입력창·창) ·
  잉크 `#22304a`(text-ink, 글씨·주요 버튼) · 골드 `#a97c2f`(text-gold, 포인트·포커스·별)
- `globals.css` 의 `@theme` 에서 Tailwind `zinc` 계열을 따뜻한 베이지 톤으로 바꿔 두었다.
  그래서 회색이 필요하면 계속 `zinc-*` 를 쓰면 된다. **`bg-white` 는 쓰지 말 것**(→ `bg-paper`).
- 글꼴 Pretendard (CDN). 첫 화면·로그인·공유 페이지 제목 위에 골드 `LIKEWAY DOCBOX` 라벨.
- 폴더 아이콘은 밝은 골드 그라데이션 + 살짝 보이는 종이 (어두운 겨자색은 칙칙하다고 했다).
- 아이콘은 이모지 대신 직접 그린 SVG: `folder-icon` `file-icon` `back-icon` `kakao-icon`.
- 창은 `sheet-in`, 어두운 배경·메뉴는 `fade-in` 애니메이션. 카톡 버튼만 카카오 노랑 유지.
- 폰 머리줄: 1줄 [뒤로·제목·경로·개수] / 2줄 검색 / 3줄 [태그(밀기) + 정렬·선택].
  문서가 없으면 정렬·선택을 숨긴다.
- 업로드 화면은 방금 보던 폴더를 기본으로 고르고(`/upload?f=`), 저장 후 그 폴더로 돌아간다.

## 규칙
- **로컬 개발 서버와 실제 사이트는 같은 Supabase 를 쓴다. 사용자가 실제 제안서를 올려 쓰고 있다.**
  테스트 문서는 반드시 `node scripts/test-data.mjs add …`(빈 파일) 또는 `… addfile <폴더> <경로> "제목"`(진짜 파일,
  영상 확인용) 으로 넣고 `… clean` 으로 지울 것
  (memo="__TEST__" 표시가 있는 것만 지운다). 조건 없이 documents/folders 를 지우는
  코드는 어떤 경우에도 실행하지 말 것. 사용자의 파일이 이렇게 한 번 지워진 적이 있다.
- 설명은 비개발자 기준으로 쉽게. 명령어는 복사해서 붙이면 되게 한 줄씩.
- 파일은 무조건 Supabase Storage에 직접 저장. 구글드라이브·드롭박스 등 외부 저장소 연동 금지.
- 라이브러리 추가는 최소한으로. 상태관리·UI 프레임워크 추가 금지 (Tailwind만).
- 한 마일스톤에서 다음 마일스톤 기능 미리 만들지 말 것.
- 막히면 우회하지 말고 무엇이 막혔는지 먼저 알려줄 것.
- 모든 문구는 한국어.
- **글씨를 작게 쓰지 말 것.** 사용자가 노안이다. 본문은 최소 `text-lg`(18px),
  보조 문구도 `text-base`(17px) 이상. `text-xs`·`text-sm` 은 쓰지 않는다.
  html 기본 크기는 106.25%(17px)로 올려둠. 손가락 확대(pinch zoom)를 막지 말 것.
- `globals.css` 의 기본 규칙은 반드시 `@layer base` 안에 둘 것. 바깥에 두면 레이어 없는 규칙이
  Tailwind 유틸리티를 이겨서 `text-xs` 같은 글씨 크기가 전부 무시된다 (버튼에서 실제로 겪었다).
- **PC 에서도 쓴다.** 폰용 화면을 그대로 늘리지 말 것. `sm:` 부터는 PC 기준으로:
  본문 폭 제한(max-w-4xl), 여백을 줄여 촘촘하게, 마우스 hover 반응,
  업로드·새 폴더는 상단 툴바 버튼(떠 있는 + 는 폰 전용), 아래에서 올라오는 창은 가운데 작은 창,
  Esc 로 닫기, 파일은 끌어다 놓기 가능. 글씨 크기는 PC 에서도 줄이지 말 것.
- **PC 목록은 윈도우 파일탐색기처럼.** 폴더와 문서를 한 목록에 한 줄씩:
  [아이콘] [이름 ............] [날짜]. 태그·용량은 감추고, 카톡·점세개 버튼은
  마우스를 올렸을 때만 나타난다(단, `@media (hover:hover)` 인 기기에서만 숨긴다 —
  태블릿·폰 가로 화면에서는 항상 보여야 한다). 폴더 타일 격자는 폰 전용.
- PC 목록 아래 남은 공간 가운데에 **올리기 영역**(점선 상자)을 둔다.
  끌어다 놓으면 지금 보고 있는 폴더로 바로 올라가고(제목은 파일명),
  누르면 파일 선택. 태그·메모까지 넣으려면 상단 [업로드] 버튼으로 간다.

## 재미 요소: 픽셀 고양이 (`app/cats.tsx`, `lib/cat-sprites.ts`)
- 자료실 화면 맨 아래를 돌아다니는 두 마리는 **사용자의 실제 고양이 태리·제리**다.
  - 태리(`taeri`): **통통하다** — 넓고 둥근 얼굴, 볼살, 넓적한 턱, 두툼한 몸.
    연한 크림·살구색, 줄무늬는 아주 은은하게, 흰 가슴과 발, 호박색 눈, 분홍 코
  - 제리(`jeri`): **얄쌍하다** — 좁은 얼굴, 뾰족한 턱(V라인), 쫑긋 긴 귀, 날씬한 몸.
    크림빛 몸에 **회색** 포인트(귀·눈가·꼬리), 이마 가운데 밝은 줄, 흰 주둥이와 가슴, 하늘색 눈
  - 두 마리는 색뿐 아니라 **머리·몸 그림이 따로**다(`FRAMES[kind]`). 색이나 모양을 바꿀 때는 이 특징을 지킬 것 (진한 주황 치즈·짙은 갈색 샴으로 되돌리지 말 것). 누르면 이름 + ♥ 가 뜬다.
- 그림 파일 없이 글자 격자로 그린 도트(SVG), 가로 30 × 세로 18칸. 부품(머리·몸·다리·꼬리)을 조립하는
  `scripts/make-cat-sprites.mjs` 가 `lib/cat-sprites.ts` 를 만든다 — 그림을 고칠 땐 스크립트를 고쳐서 다시 실행.
- 걷기·앉기·꼬리 흔들기·그루밍·기지개(자다 깨면 먼저 켠다)·잠(z)·서로 덮치고 쫓기(!)·자는 애 깨우기/같이 자기.
  밤(22~7시)에는 더 많이 잔다. 누르면 ♥ + 폴짝.
- **사냥놀이·우다다** (실제로 둘이 그렇게 논다): 한 마리가 멀찍이서 납작 엎드려 엉덩이를 씰룩(`stalk`) →
  사냥감 바로 앞에 떨어지도록 도약(`pounce`) → 놀란 애가 `!` 하고 튀며 둘 다 우다다(`zoom`, 화면 끝에서 끝까지
  전속력 왕복, 쫓는 애는 벽 조금 앞에서 돌아 뒤를 따라감) → 지쳐서 털썩 앉거나 그루밍. 가끔 이유 없이 우다다도 한다.
- **앱 동작에 반응** (`lib/cat-events.ts` 의 `tellCats`): 파일을 올리면 `!` 하고 가운데로 달려와 마주 앉아 구경
  (구경 중에는 서로 장난을 걸지 않는다), 카톡을 보내면 둘 다 제자리에서 폴짝폴짝 + ♥.
  업로드 화면에서 돌아올 때는 sessionStorage 로 소식을 넘긴다.
- **밥그릇·물그릇**: 배고프면(hunger) 밥그릇, 목마르면(thirst) 물그릇으로 가서 고개 숙여 먹는다.
  먹을 때마다 한 칸씩 줄고(0~3, localStorage `docbox-bowls` 에 기억), 비면 안 먹는다.
  **그릇을 누르면 가득 채워지고** 배고픈 애들이 `!` 하며 달려온다. 태리는 왼쪽·제리는 오른쪽에 서서 마주 보고 먹는다.
- **주인 이윤**(긴 갈색 머리, 분홍 니트, 남색 치마): **밥·물을 채우거나 츄르를 줄 때만 나온다. 알아서 들르지 않는다**
  (사용자가 그렇게 해 달라고 했다). 그릇은 저절로 채워지지 않고, 이윤이 가까운 쪽 가장자리에서 걸어 들어와
  쪼그려 앉아 **사료 봉지 / 생수병**을 그릇 위로 기울여 따라 준 뒤(알갱이·물이 떨어지는 그림 2장을 번갈아)
  손 흔들고 나간다. 여러 개를 연달아 시키면 차례로 한다.
  츄르를 주면 둘 다 달려와 줄 서서 먹고, 다 먹으면 하트·폴짝. 이윤을 누르면 "이윤 ♥".
- **쓰다듬기**: 이윤이 걸어와 쪼그려 앉아 **팔을 위로 뻗어 고양이 머리에 손을 얹는다**
  (쪼그린 이윤의 얼굴과 앉은 고양이의 머리 높이가 비슷해서, 팔은 어깨보다 위로 올라가야 닿는다 —
  `O_TORSO_PET_A/B` 는 윗줄이 팔뿐이라 `composeOwner` 에서 y=12 에 놓는다).
  태리가 손 바로 앞, 제리가 그 뒤에 앉아 **둘 다 눈을 감고**(`nuzzleA/B`) 머리를 비비며 이따금 ♥.
  꼬리는 바닥에 둔 채 한 칸만 들썩인다(세웠다 내리면 뚝뚝 끊겨 보인다). 다 하면 손 흔들고 나간다.
- **같이 낮잠자기** — **버튼을 빼 뒀다**. 자는 모습이 어색해서 사용자가 쓰다듬기로 바꿔 달라고 했다.
  동작(`Task "nap"`)은 `app/cats.tsx` 에 그대로 살아 있으니, 그림을 고친 뒤
  `app/cat-care.tsx` 의 `ITEMS` 에 다시 넣기만 하면 된다: 이윤이 **베개를 품에 안고** 걸어와 화면 한가운데 눕고(베개 벤 가로 그림),
  양옆으로 태리·제리가 와서 쿨쿨(셋 다 z). 자는 동안은 눌러도 안 깨고 장난도 안 친다 →
  다 자면 고양이들은 기지개, 이윤은 손 흔들고 나간다.
- **놀아주기**: 이윤이 낚싯대 장난감(분홍 깃털)을 들고 와 흔든다 → 둘이 깃털 아래로 달려와 **번갈아 폴짝** →
  다 놀면 하트와 함께 신나서 우다다 한 바퀴 → 이윤은 손 흔들고 나간다. 낚싯대는 이윤의 든 손에서 뻗어 나오고
  **고양이보다 앞에** 그린다(깃털이 머리 뒤에 숨지 않게).
- 그루밍할 때 든 앞발은 **가슴에서 비스듬히 올라와 턱에 닿아야** 한다 — 몸에서 떨어져 보이면 안 된다(제리에서 지적받음).
- **밥·물은 평소에 채워져 있다.** 고양이가 먹을 때마다 한 칸씩 줄고(한 마리가 3~4분에 한 번 — 앱을 켜 둔 시간 기준.
  시계 기준으로 바꾸자는 안은 사용자가 거절: "재미 요소니까 원래대로"), 다 비면
  배고픈 고양이가 빈 그릇 앞에 앉아 `?` 하며 기다린다. **채우기는 비었을 때만** 된다(남아 있으면 버튼도 없고 그릇을 눌러도 무반응).
- **홈 화면 돌보기 버튼**(`app/cat-care.tsx`): 화면을 깔끔하게 — 제목·안내 글 없이 **오른쪽 아래 아주 작은 알약 버튼**만
  (높이 30px · 너비 62px **다섯 개 모두 같은 크기·글씨·굵기**, `text-xs` + `font-semibold`, 한 줄 고정.
  글자는 밥/물/츄르/놀이/쓰다듬 으로 짧게 — 다섯 개가 폰 한 줄에 들어가야 한다.
  재미 요소라 여기만은 '글씨 작게 쓰지 말 것' 규칙의 예외 — 사용자가 더 줄여 달라고 했다).
  [밥]·[물] 은 **그 그릇이 비었을 때만** 골드색으로 나타난다. [츄르]·[놀이]·[쓰다듬] 은 항상 있다.
  다섯 개가 폰 한 줄에 들어가야 한다 (430·393·360·320px 에서 한 줄·글자 안 잘림 확인).
  밥그릇·물그릇은 **왼쪽 아래 구석**에 있고, 바닥에 츄르 아이콘은 두지 않는다(츄르는 이윤 손에서만 보인다). 그릇 상태는 `docbox:bowls` 이벤트로 받고,
  누르면 `careForCats()` → `docbox:care` 로 이윤을 부른다. 홈(첫 화면)에만 둔다.
- **기능을 방해하면 안 된다**: 띠는 `pointer-events-none`, 고양이만 눌림. 홈·올리기 버튼(z-20)보다 뒤(z-10).
  고양이 칸은 아이폰 홈 바에 잘리지 않게 `bottom: calc(env(safe-area-inset-bottom,0px) + 20px)` 로 띄운다.
  여러 개 선택 중(하단 바)에는 숨긴다. 탭이 안 보이면 멈춘다.
  **`prefers-reduced-motion` 으로 고양이·돌보기 버튼을 끄지 말 것** — 사용자 윈도우가 애니메이션 끔 설정이라
  사용자 본인 화면에서 기능이 통째로 사라졌었다.

## PWA (홈 화면에 추가)
- `app/manifest.ts` → `/manifest.webmanifest`. 홈 화면 이름은 **자료실**(short_name), 전체 이름은 라이크웨이 자료실
- 아이콘: 크림(라이크웨이 톤) 바탕 + **태리와 제리가 나란히 앉은 픽셀 그림**
  (폴더 그림은 별로라고 해서 바꿨다. 남색 바탕도 칙칙하다고 해서 쓰지 않는다).
  `node scripts/make-icons.mjs` 가 `lib/cat-sprites.ts` 의 앉은 모습을 그대로 읽어 만든다 —
  아이콘을 고칠 땐 이 스크립트를 고쳐서 다시 실행할 것.
  결과: `public/icons/`(192·512·maskable), `app/icon.png`, `app/apple-icon.png`
- 아이콘·manifest 는 로그인 없이 열려야 한다 (`proxy.ts` matcher 에서 제외돼 있음)
- 서비스 워커는 두지 않는다 — 로그인·실시간 목록 앱이라 캐시가 낡는 문제가 더 크다

## 완료 기준
폰에서 앱 열기 → 검색창에 "그라인드" 입력 → 나온 제안서의 [공유] 누르기 → 카톡 대화방 선택 → 파일 전송. **이 과정이 10초 안에 되면 끝.**
